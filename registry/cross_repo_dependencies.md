# Cross-Repository Dependency Graph

This document records cross-repository dependency relationships inside the Integrity Nexus ecosystem.

The map is intentionally conservative.

It does not claim that one repository proves another.

---

# Dependency Classes

```text
D0 — navigation reference
D1 — shared terminology
D2 — conceptual analogy
D3 — governance dependency
D4 — evidence-supported dependency
D5 — formal dependency
```

All current cross-repository dependencies are D1-D3 unless explicitly upgraded.

---

# Global Research Map

```text
Integrity Nexus
│
├── TIG — bounded physical/geometric structure
│
├── SIR — bounded recursive/governance structure
│
└── SSC — bounded control-state structure
```

Shared denominator:

```text
Integrity-preserving bounded structural evolution
```

---

# Structural Dependency Pattern

```text
Shared Concept
  ↓
Domain Projection
  ↓
Repository-Specific Claim
  ↓
Repository-Specific Evidence / Proof / Open Problem
```

---

# TIG ↔ SSC

Shared concept:

```text
Admissible bounded transition
```

TIG projection:

```text
Bounded structural transition in gravitational/geometric settings
```

SSC projection:

```text
Bounded state transition in control architectures
```

Dependency class:

```text
D2 — conceptual analogy / structural alignment
```

Boundary:

```text
TIG does not prove SSC.
SSC does not prove TIG.
```

---

# SIR ↔ SSC

Shared concept:

```text
Recursive integrity under governance constraints
```

SIR projection:

```text
Research and governance recursion
```

SSC projection:

```text
Control-state recursion and supervisor-independent boundaries
```

Dependency class:

```text
D3 — governance dependency
```

Boundary:

```text
SIR provides governance methodology.
SSC provides control-boundary research.
```

---

# TIG ↔ SIR

Shared concept:

```text
Structural integrity under constrained evolution
```

TIG projection:

```text
Mathematical physics and geometric structure
```

SIR projection:

```text
Recursive integrity and research-process control
```

Dependency class:

```text
D1/D2 — shared vocabulary and conceptual alignment
```

Boundary:

```text
The repositories remain domain-separated.
```

---

# SSC Governance Dependency

```text
SSC
  ↓ depends on
SIR-style registry and governance methodology
  ↓ governed by
Integrity Nexus repository standard
```

Dependency class:

```text
D3 — governance dependency
```

Status:

```text
active
```

---

# TIG Governance Dependency

```text
TIG
  ↓ governed by
Integrity Nexus claim-boundary standard
  ↓ must preserve
physics-domain claim separation
```

Dependency class:

```text
D3 — governance dependency
```

Status:

```text
alignment pending detailed audit
```

---

# SIR Meta-Governance Dependency

```text
SIR
  ↓ informs
Integrity Nexus governance architecture
  ↓ receives
Nexus maturity and repository standards
```

Dependency class:

```text
D3 — reciprocal governance dependency
```

Status:

```text
alignment pending detailed audit
```

---

# Current Status

Expanded cross-repository dependency graph.

All dependencies remain conceptual or governance-level unless a repository-specific proof or evidence object explicitly upgrades the dependency class.

---

# Homo Transcendens Cross-Repository Thread

Master routing record:

`registry/homo_transcendens_cross_repo_routing.md`

Current scientific owner:

~~~text
Structural_State_Controller/research/homo_transcendens/
~~~

Dependency graph:

~~~text
TIG-E historical HT provenance
        ↓ D0/D4 provenance/evidence input
Structural_State_Controller / HT
        ↑ D1/D4 typed Foundation dependency
Ex-I-Greater-Than-Zero / Foundation

Structural_State_Controller / AI Alignment
        ↔ D0/D1 sibling boundary only
Structural_State_Controller / HT
~~~

Bound navigation source:

`Structural_State_Controller/research/homo_transcendens/HT_CROSS_REPO_SOURCE_AND_DEPENDENCY_MAP_v0.1_2026-09-18.md`

Bound historical source:

`TIG-E-Topological_integrity-_gravity_engine-@636b2d4320e131c67a75fc1ebbdacabe1dfb9ba5`

Bound Foundation snapshot:

`Ex-I-Greater-Than-Zero@1ff2c43fa624c9bde0abdfdac4c59e96ce7ddde5`

Boundary:

~~~text
historical provenance != current owner
Foundation import != Foundation redefinition
Alignment boundary input != HT claim ownership
HT repair readiness != HT scientific proof
~~~

Current AIL-2 repair status is controlled locally by:

`Structural_State_Controller/research/homo_transcendens/audits/HT_AIL2_REPAIR_TRACEABILITY_v0.1_2026-09-18.md`

No dependency-class entry here promotes any HT, Foundation, Alignment, Superaddition, Emergence, HT-MAX, replication, or generalization claim.
