name: DISABLED - legacy GitHub Actions workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow intentionally disabled by repository owner."
