# Cross-Repository Dependency Graph v1.0

## Status

Architecture Registry / Cross-Repository Dependency Graph

## Maturity

L2 Repository Registered

## Classification

Integrity Nexus / Architecture / Dependency Graph / Cross-Repository Governance

## Claim Type

Dependency architecture document only.

This document introduces no mathematical claims.
It does not prove or validate any theory.
It does not assert mathematical dependency unless separately proven.
It records visible repository-level dependency families.

---

# Purpose

This document records the current cross-repository dependency graph for the eight-repository Integrity Nexus research architecture.

It distinguishes:

```text
repository dependency
```

from:

```text
mathematical derivation dependency
```

---

# Mandatory Boundary

```text
Repository dependency != mathematical derivation
```

```text
Architecture edge != proof edge
```

```text
Roadmap dependency != scientific closure
```

---

# Repository Nodes

```text
Integrity_Nexus
TIG-E-Topological_integrity-_gravity_engine-
Quantum_Integrity_Core
Structural_Integrity_Recursion
Structural_State_Controller
TIG_YM_derivation_architecture
TIG_YM_Research
integrity-nexus-kai.github.io
```

---

# Primary Dependency Edges

## D-001 — Integrity_Nexus -> All Repositories

Type:

```text
Governance / roadmap / architecture framing
```

Meaning:

```text
Integrity_Nexus provides meta-governance, roadmap, and cross-repository architecture context.
```

Boundary:

```text
Meta-governance does not prove domain claims.
```

---

## D-002 — TIG-E -> Integrity_Nexus

Type:

```text
Status export / scientific blocker export
```

Meaning:

```text
TIG-E exports bounded registry status, Cube status, field-equation blocker status, and QIC/QM boundary status to the meta layer.
```

Boundary:

```text
TIG-E status export does not automatically update the global roadmap without registration.
```

---

## D-003 — Quantum_Integrity_Core <-> TIG-E

Type:

```text
QIC/TIG formalization interface
```

Meaning:

```text
Quantum_Integrity_Core and TIG-E share QIC/TIG-adjacent concepts but require explicit interface mapping before treating definitions as shared.
```

Boundary:

```text
Shared terminology != shared source-of-truth.
```

---

## D-004 — Structural_State_Controller <-> TIG-E

Type:

```text
Evolution generator / Hamiltonian foundation blocker alignment
```

Meaning:

```text
SSC missing evolution generator and TIG-E/QIC missing Hamiltonian foundation are aligned as one unresolved blocker family at architecture level.
```

Boundary:

```text
Alignment != generator derivation.
```

---

## D-005 — Structural_Integrity_Recursion <-> TIG-E

Type:

```text
Toy sequence / recursion / manifold bridge
```

Meaning:

```text
TIG-E toy model sequence may require SIR bridge work for recursive and manifold-level generalization.
```

Boundary:

```text
Toy-to-SIR bridge != full manifold derivation.
```

---

## D-006 — TIG_YM_Research -> TIG_YM_derivation_architecture

Type:

```text
Research-to-architecture promotion path
```

Meaning:

```text
Private TIG/YM research material may be promoted into theorem-oriented derivation architecture after review.
```

Boundary:

```text
Research note != theorem architecture.
```

---

## D-007 — TIG_YM_derivation_architecture -> Integrity_Nexus

Type:

```text
Proof-obligation status export
```

Meaning:

```text
YM theorem architecture may export proof-obligation and dependency status to the meta layer.
```

Boundary:

```text
Proof obligation != proof completion.
```

---

## D-008 — Domain Repositories -> integrity-nexus-kai.github.io

Type:

```text
Public summary import path
```

Meaning:

```text
The public website may import bounded summaries from canonical repositories.
```

Boundary:

```text
Public presentation != source of truth.
```

---

# High-Risk Dependency Zones

```text
Hamiltonian foundation / evolution generator
```

```text
Cube state space / QIC state space interface
```

```text
Toy sequence / SIR / manifold bridge
```

```text
QIC formalization / TIG-E QIC-QM boundary status
```

```text
YM research / YM derivation architecture promotion
```

```text
public website / claim-boundary synchronization
```

---

# Required Future Audits

```text
GATE-META-DEPENDENCY-DETAIL-001
TITLE: Cross-Repository Dependency Edge Verification Audit
OUTPUT: architecture/cross_repository_dependency_edge_verification_audit.md
```

```text
GATE-META-DRIFT-001
TITLE: Cross-Repository Status Drift Detection Audit
OUTPUT: architecture/cross_repository_status_drift_detection_audit.md
```

---

# Locked Verdict

```text
CROSS-REPOSITORY DEPENDENCY GRAPH:
REGISTERED
```

```text
DEPENDENCY EDGES REGISTERED:
8
```

```text
PRIMARY BOUNDARY:
dependency edge != proof edge
```

Repository status:

```text
CROSS-REPOSITORY DEPENDENCY GRAPH REGISTERED
```
