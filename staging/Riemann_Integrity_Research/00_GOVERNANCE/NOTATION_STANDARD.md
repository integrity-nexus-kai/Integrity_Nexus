# Notation Standard — Initial

**Status:** INITIAL / TO BE RECONCILED WITH PRIMARY SOURCES

The notation below is provisional and exists to prevent drift during Phase 0. It must be checked against the baseline sources before canonical use.

| Symbol | Provisional meaning | Status |
|---|---|---|
| `s = σ + it` | complex variable | provisional standard |
| `ζ(s)` | Riemann zeta function | provisional standard |
| `σ = Re(s)` | real part | provisional standard |
| `t = Im(s)` | imaginary coordinate | provisional standard |
| `ρ` | nontrivial zero when context is explicit | provisional standard |
| `π(x)` | prime-counting function | provisional standard |
| `Λ(n)` | von Mangoldt function | provisional standard |
| `ψ(x)` | Chebyshev psi function | provisional standard |

## Rule

No symbol imported from TIG, SIR, Cube, Consciousness, or another Nexus repository may be reused in the Riemann mathematical baseline without a collision check and explicit definition.